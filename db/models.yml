LoyaltyProgram:
  store: string
  program: string
  program_num: integer

User:
  first_name: string
  last_name: string
  middle_initial: string
  dob: string
  income: integer
  loyaltyProgram_id: integer
  gender: string
  hh_size: integer
  address1: string
  address2: string
  city: string
  state: string
  zip: string
  email: string #Confirm with Brian
  race: string
  language: string
  marital_status: string
  kids_under_18: integer
  education: string
  HH_id: string #Need to think if this makes sense to add--useful for multiple people in same HH 
  #w diff buying habits

Coupon:
  loyaltyProgram_id: integer
  discount: integer
  discounted_item: integer #makes most sense it will be some interger 
  discounted_category_num: integer 
  discounted_category_str: string
  coupon_code: string #Not sure? Img file? String? 

Transaction:
  ser_id: integer
  loyaltyProgram_id: integer
  item_bought: integer
  price: integer
  qty_bought: integer
  coupon_id: integer
  #Make more detailed and collect every item? Would make sense. But how to collect? 
  #What if they buy a lot of items (e.g. 20 things at grocery store)?
